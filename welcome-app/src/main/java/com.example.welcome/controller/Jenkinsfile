pipeline {
    agent any
    stages {
        stage('Checkout Code') {
            steps {
                // Checkout the code from Git SCM
                checkout scm
            }
        }
    }
}